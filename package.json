{
  "name": "json-server",
  "version": "1.0.0",
  "description": "simple-json-server",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch db.json --port 8000"
  }
}
